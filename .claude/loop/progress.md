# Loop Progress — the checkpoint blackboard

> Shared state across iterations. Each headless agent is stateless and rebuilds
> context from THIS file + spec.md. Keep it terse and append-only within a run.
> The loop driver appends iteration headers automatically.

## STATUS
NOT_STARTED

<!-- The review phase writes exactly the token LOOP_COMPLETE on its own line here
     when, and only when, every acceptance criterion is met. The driver greps for
     it. Do not write that token for any other reason. -->

## PLAN (filled by the planner phase)
_(empty — planner will populate from spec.md)_

## LOG
