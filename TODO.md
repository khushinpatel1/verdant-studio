# Verdant Studio — Backlog & Decisions

> Work-list. Finish = check. Defer = write. Intent: `VISION.md`. Rules: `CLAUDE.md`.

## Active / pre-ship
- [x] **007 — Site ship plan** ([plan](../verdant-studio/.claude/plans/007-ship-plan.md)). All 7 phases done. Live.
  - [ ] **KP pre-ship calls:** CTA final pick (3 options swap-ready). Hero assets upgrade (video + screenshots). Pricing terms (placeholder → real or removed).

## Quality backlog
- [ ] **CSP hardening.** Swap `'unsafe-inline'` to hash-based CSP via `experimental.csp`. 20-min job. Low priority (site works now).

## Blocked / gated
- [ ] **Garden pricing — business-mode scrutiny** (KP + Codex). Model: F&F lifetime → low flat + $4/mo hosting + sync passthrough.

## Decision log — active
| Date | Decision |
|---|---|
| 2026-06-20 | **v3 only.** Retired v1 (dark) + v2 (luminous) CF projects. Single source: `verdant-studio-v3`. |
| 2026-06-09 | **Studio reset.** Renamed from `pastures` (multi-skin) to standalone `verdant-studio`. Only v3 lives; ridge/meadow/feral archived. |

## Locked decisions
See plan files + decision log below (full record in `.claude/plans/*`).

## Depth
- Per-project backlog: `verdant-studio/TODO.md` (this file).
- Plan files: `.claude/plans/` (003–015).
- Done (recent): see `.claude/plans/*` headers or `partnership/DONE-LOG`.
